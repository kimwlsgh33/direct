<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   
<mapper namespace="com.linker.test.mapper.memberMapper">

	<!--회원 목록 가져오기 --> <!--parameterType="int"-->
	<select id="selectMember" resultType="com.linker.test.vo.MemberVO">
		<![CDATA[
		SELECT * FROM direct ORDER BY id
		]]>
	</select>

</mapper>